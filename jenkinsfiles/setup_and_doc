pipeline {
  agent { label 'install-and-setup-agent' }
  stages {

    stage('Build'){
      steps{
        sh '''
          ./setup.sh --docs --force
          ./setup.sh -i --force
        '''
      }
    }

  }
}

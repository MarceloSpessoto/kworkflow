pipeline {
  agent { label 'basic-agent' }
  stages {

    stage('Build'){
      steps{
        sh '''
          ./setup.sh --docs --force
          ./setup.sh -i --force
        '''
      }
    }

  }
}

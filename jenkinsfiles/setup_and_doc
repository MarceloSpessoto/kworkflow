pipeline {
  agent { dockerfile true }
  stages {

    stage('Build'){
      steps{
        sh '''
          apt install -y python3-docutils python3-sphinx graphviz
          ./setup.sh --docs --force
          ./setup.sh -i --force
        '''
      }
    }

  }
}

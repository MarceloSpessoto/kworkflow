pipeline {
  agent { label 'docker-agent' }
  stages {

    stage('Build'){
      steps{
        sh '''
          sudo apt update -y
          sudo apt install -y shfmt
          shfmt -i=2 -ln=bash -fn -ci -sr -d .
        '''
      }
    }

  }
}

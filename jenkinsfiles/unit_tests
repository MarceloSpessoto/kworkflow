pipeline {
  agent { label 'basic-agent' }
  stages {

    stage('Build'){
      steps{
        sh '''
          sudo ./setup.sh -i --force
          ./run_tests.sh --unit
        '''
      }
    }

  }
}
